# ベース仕様: ネットワーク層 HTTP リクエスト

## 概要

HTTP プロトコルを用いたリモートリソース取得の仕様。

## API 仕様（変更前）

- 指定されたパスに対して HTTP GET リクエストを送信する。
- レスポンスボディの内容を out_buffer にコピーし、書き込んだバイト数を返す。
- 設定が不正な場合、path が NULL の場合、バッファが不足する場合は -1 を返す。
