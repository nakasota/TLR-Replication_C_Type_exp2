# 変更後仕様: TCP クライアント接続管理

## 概要

TCP ソケットを用いたネットワーク接続の確立・解放を行う。

## 仕様（変更後）

- ホストとポートを指定して TCP 接続を開く。
- **変更点**: port が 0 以下の場合に加え、port が 65535 を超える場合も -1 を返す（不正なポート番号の検証強化）。
- config が不正、host が NULL の場合は -1 を返す。
- 成功時は接続ハンドル（正の整数）を返す。
