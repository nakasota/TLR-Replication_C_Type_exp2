# 変更後仕様: 数値演算ユーティリティ

## 概要

基本的な算術演算を提供するユーティリティ。

## 仕様（変更後）

- 2 つの整数 a, b を受け取り、a から b を引いた差を返す。
- **変更点**: b が a より大きい場合（結果が負になる場合）は 0 を返す（符号なし演算として扱う）。
- 例: sub(10, 3) は 7、sub(3, 10) は 0 を返す。
- オーバーフローは考慮しない。
