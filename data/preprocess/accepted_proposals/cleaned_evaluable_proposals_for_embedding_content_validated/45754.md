flag: add textvar handl type implement encoding.textunmarshal
`encoding.textmarshaler` `encoding.textunmarshaler` interfac de-facto way type self-report serial to/from humanli readabl text format.
easiest way tie `flag` packag `encoding.textunmarshaler` to:
1. use `flag.func` declar anonym function call unmarsh function hood. approach take >3 lines, unfortun display default valu usag documentation.
2. declar type implement `flag.value` use `flag.var`. approach requir declar 1 type 2 method gener 10 line cost.
way detriment (either long drop default valu printed).
propos adding:
```go
// TextVar defines a flag with a specified name, default value, and usage string.
// The argument p points to a variable in which to store the value of the flag.
// The flag accepts a value according to encoding.TextUnmarshaler.UnmarshalText.
// The default value type must be the same as the pointed at variable type.
func TextVar(p encoding.TextUnmarshaler, name string, value encoding.TextMarshaler, usage string)
```
along equival method `flagset` type.
exampl usag would be:
```go
var t time.Time
flag.TextVar(&t, "start_time", time.Now(), "Time to start processing at.")
```

```go
var n big.Int
flag.TextVar(&n, "n", big.NewInt(3853882583591558728), "Run integer factorization for this number")
```
note, approach requir singl line (other variabl declaration) work type implement `encoding.textmarshaler` `encoding.textunmarshaler`.
type safeti loss `p` argument `value` argument may concret type. gener could resolv problem.
---
within standard library, 5 type implement interfaces:
* `"math/big".float`
* `"math/big".int`
* `"math/big".rat`
* `"net".ip`
* `"time".time`
one could conceiv use appli `flag` package.
accord latest version modules, ~14k type implement `encoding.textunmarshaler` ~8.2m type total. account 0.2% types, still non-trivi number implementations.
chang mention issue: `flag: add textvar function`
clever. often found peopl reach third-parti flag librari sake type built-in implement `flag.value` manually.
could also nudg librari toward implement `textmarshaler` `textunmarshaler`, think that' good thing. encod packag rare use directly, think go develop even well awar "text encoding" interface. librari would rare go way implement `flag.value` directly, implement two text method much reason reusable.
could use today ad flag control logrus.level service.
/cc @robpik
propos ad activ column propos project
review weekli propos review meetings.
— rsc propos review group
seem reason me. might abl forestal futur requests.
base discuss above, propos seem like **like accept**.
— rsc propos review group
chang consensus, **accepted**. 🎉
