net/http/httputil: add empti x-forwarded-for header reverseproxi
### version go use (`go version`)?

$ go version
go version go1.13.8 linux/386

### issu reproduc latest release?
ye
### oper system processor architectur use (`go env`)?
<details><summary><code>go env</code> output</summary><br>
$ go env
go111module="on"
goarch="386"
gobin="~/go/bin/"
gocache="~/.cache/go-build"
goenv="~/.config/go/env"
goexe=""
goflags=" -mod=vendor"
gohostarch="386"
gohostos="linux"
gonoproxy=""
gonosumdb=""
goos="linux"
gopath="~/go"
goprivate=""
goproxy="direct"
goroot="/usr/local/go1.13"
gosumdb="sum.golang.org"
gotmpdir=""
gotooldir="/usr/local/go1.13/pkg/tool/linux_386"
gccgo="gccgo"
go386="sse2"
ar="ar"
cc="gcc"
cxx="g++"
cgo_enabled="1"
gomod="~/git/go.mod"
cgo_cflags="-g -o2"
cgo_cppflags=""
cgo_cxxflags="-g -o2"
cgo_fflags="-g -o2"
cgo_ldflags="-g -o2"
pkg_config="pkg-config"
gogccflags="-fp -m32 -pthread -fmessage-length=0 -fdebug-prefix-map=/tmp/go-build275301714=/tmp/go-build -gno-record-gcc-switches"
</details>
### do?
simpli use `reverseproxy` ( implement http proxy. default, documented, `reverseproxi automat set client ip valu x-forwarded-for header. x-forwarded-for header alreadi exists, client ip append exist values.`. situation, proxi transpar invis servers, like proxi add header http request forwards. unfortunately, option disabl addit `x-forwarded-for` header. unless mistaken, also way remov header manual messag forward (like hook someth call point).
### expect see?
option `reverseproxy` struct disabl `x-forwarded-for` header. example, someth like `disableforwardedforhead bool`, would `false` per default, mean behavior stay like now. `true`, proxi set `x-forwarded-for` header, through, e.g., `if !p.disableforwardedforheader` line 246
anoth option would add hook modifi request proxi sends/forward it. would actual eleg gener solution. could current `director` hook, call later `servehttp` function, i.e., `transport.roundtrip(outreq)`. sure would bad impact. case, anoth hook possible.
### see instead?
option proxi alway set `x-forwarded-for` header.
discuss @bradfitz. look like header["x-forwarded-for"] present nil (that is, miss map entirely, empty) today, end "x-forwarded-for: , 1.2.3.4" (with empti string comma). could redefin present nil mean add anyth header. elsewher net/http already, user might tri even without docs. mayb that' way forward?
think could make sense. especi current situat "x-forwarded-for: , 1.2.3.4" header realli make sense.
sound like propos two comment (don't add already-empti x-forwarded-for) would fix problem peopl least objecting. retitled.
anyon object this?
base discussion, seem like **like accept**.
chang mention issue: `net/http/httputil: append x-forwarded-for reverseproxi nil`
chang mention issue: `net/http/httputil: append x-forwarded-for reverseproxi nil`
chang consensus, accepted. (cl commit already.)
