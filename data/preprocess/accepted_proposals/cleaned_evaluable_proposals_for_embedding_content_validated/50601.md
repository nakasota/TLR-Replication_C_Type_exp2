encoding/binary: add appendbyteord
common oper deal binari wire format need append fixed-width integer.
currently, common way with:
```go
var arr [4]byte
binary.LittleEndian.PutUint32(arr[:], val)
out = append(out, arr[:]...)
```
take 3 line cannot done expression.
propos addit new interface:
```go
type AppendableByteOrder interface {
	AppendUint16([]byte, uint16) []byte
	AppendUint32([]byte, uint32) []byte
	AppendUint64([]byte, uint64) []byte
	String() string
}
```
document `littleendian` `bigendian` implement `byteorder` `appendablebyteorder`.
thus, line simplifi as:
```go
out = binary.LittleEndian.AppendUint32(out, val)
```
work variou binari wire format (e.g., tar, protobuf, etc.), api like would conveni exist `putuintx` apis.
`appendablebyteord ` interfac exist primarili document purposes. strictli needed.
\cc @josharian
assum `appenduint16([]byte, uint16) []byte` etc
> assum appenduint16([]byte, uint16) []byte etc
correct. thank catch that. fix origin proposal.
propos ad activ column propos project
review weekli propos review meetings.
— rsc propos review group
recent thing like readfilef fs readfil method.
`type appendbyteorder`?
-abl seem place go.
otherwis seem reasonable, nice littleendian bigendian unexport type add method use directly.
base discuss above, propos seem like **like accept**.
— rsc propos review group
chang consensus, **accepted**. 🎉
