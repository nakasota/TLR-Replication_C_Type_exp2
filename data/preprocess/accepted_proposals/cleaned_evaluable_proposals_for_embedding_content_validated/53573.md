crypto/x509: surfac reasoncod insid x509.revocationlist entri
## background
crypto/x509 packag contain varieti type repres object make pki ecosystem, certificates, ocsp request responses, crls. type repres underli asn.1 structur (e.g. certif type rawtbscertif field) collect ergonom helper abstract away asn.1 detail (e.g. certif type dnsname field, correspond subset content subjectaltnam extension).
crypto/x509/pkix sub-packag contain collect type much close tie raw asn.1 definitions, without addtion ergonom provid crypto/x509 types. type use directli (un)marshal asn.1 data.
currently, crl exist sort in-between state. x509.revocationlist type number field abstract away underli asn.1 crlnumber extension. however, actual list entri insid x509.revocationlist []pkix.revokedcertificate. mean _not_ reasoncod field abstract away underli asn.1 reasoncod extension.
## object
provid way crypto/x509 packag expos revoc reason everi entri crl, without requir user directli interfac asn.1 extensions.
## propos
add new type x509 packag repres singl crl entri extra ergonomm ad top:
```
// CRLEntry represents a single entry in a Certificate Revocation List.
type CRLEntry struct {
	SerialNumber     *big.Int
	RevocationDate   time.Time
	RevocationReason int
	ExtraExtensions  []pkix.Extension
}
```
revocationreason field would take valu crypto/ocsp.response.revocationreason field. unfortun revoc reason constant defin crypto/ocsp packag (e.g. ocsp.keycompromise) despit origin defin rfc 5280; perhap move crypto/x509 packag give type would part propos well.
add new field x509.revocationlist list new type. deprec old revokedcertif field.
```
type RevocationList struct {
	...
	// Entries is used to populate the revokedCertificates
	// sequence in the CRL, or is populated from the revokedCertificates
	// sequence when parsing a CRL. It is an error for both CRLEntries
	// and RevokedCertificates to be non-nil when passed as a template
	// to CreateRevocationList.
	Entries []x509.CRLEntry
	// RevokedCertificates is used to populate the revokedCertificates
	// sequence in the CRL, if Entries is nil.
	// DEPRECATED: use Entries instead.
	RevokedCertificates []pkix.RevokedCertificate
	...
}
```
~~i'll also note current code `master` indic `pkix.revokedcertificate` deprecated:~~

~~even though still use current `x509.revocationlist` `master`:~~

_edit: fix
cc @golang/secur
seem reasonable, unfortun need add whole new type singl field, guess also open ad conveni field road realli need to. gener make `pkix` less user face good.
relat `revocationlist` may clearer new type name `revocationlistentry` something. also implement need take care ignor zero valu `revocationreason` ad unnecessari `unspecified` extens everi entry.
yeah, particular feel name new type new field.
implement non-backwards-compat version this, includ handl zero-valu reasoncode, take look potenti implement interest:
propos ad activ column propos project
review weekli propos review meetings.
— rsc propos review group
base discuss above, propos seem like **like accept**.
— rsc propos review group
chang consensus, **accepted**. 🎉
