bufio: add writer.availablebuff
mani api go append `[]byte` return `[]byte`.
unfortunately, api oper poorli `bufio.writer` sinc type accept `[]byte` input (via `write` method) never provid `[]byte`. requir logic tri combin append-lik function `bufio.writer` respons alloc buffer, `bufio.writer` alreadi buffer.
propos addit new method provid access underli `[]byte`:
```go
// AvailableBuffer returns an empty buffer with b.Available capacity.
// This buffer is intended to be appended to and
// passed to an immediately succeeding Write call.
func (b *Writer) AvailableBuffer() []byte {
    return b.buf[b.n:][:0]
}
```
exampl usage:
```go
var bw *bufio.Writer = ...
b := bw.AvailableBuffer()
b = strconv.AppendUint(b, ...)
bw.Write(b)
```
common case `bw.available()` larg enough, `strconv.appenduint` append buffer without allocating. buffer pass `write`, underli call `copy` return immedi without actual copi sinc sourc destin identical. save us alloc copy.
rare case allocates, subsequ `write` call still work expected, need copy.
detriment api expos intern buffer `bufio.writer` previous imposs access it. however, type packag (i.e., `reader` `scanner`) alreadi provid unsaf access underli buffer via `reader.peek`, `reader.readslice`, `scanner.bytes` methods.
propos ad activ column propos project
review weekli propos review meetings.
— rsc propos review group
seem reasonable. anyon object this?
base discuss above, propos seem like **like accept**.
— rsc propos review group
chang consensus, **accepted**. 🎉
