net/http: add transport.getproxyconnecthead
net/http.transport current has:

```go
    // ProxyConnectHeader optionally specifies headers to send to
    // proxies during CONNECT requests.
    ProxyConnectHeader Header // Go 1.8
```
... static life transport.
authent mode like ntlm/kerberos/negoti requir dynam header value, current requir make new transport request go proxy. that' heavy, transport unit connect reuse.
proposal:
```go
    // GetProxyConnectHeader is called to return headers to send to
    // proxuURL during a CONNECT request to the target ip:port. If it returns an error, the Transport
    // RoundTrip fails with that error. It can return (nil, nil) to not add headers.
    // If GetProxyConnectHeader is non-nil, ProxyConnectHeader is ignored.
    GetProxyConnectHeader func(ctx context.Context, proxyURL *url.URL, target string) (Header, error)
    // ProxyConnectHeader optionally specifies headers to send to
    // proxies during CONNECT requests. For dynamic use, see GetProxyConnectHeader.
    ProxyConnectHeader Header // Go 1.8
```
potenti implementation: (licensed+cla'd use go)
(lack test now.)
get bit non-standard follow pattern `getfoo func() t` overrid `foo t` struct realiz late `foo t` might initializ start. (for example, request.getbodi vs request.body.)
anyon object this?
base discuss above, seem like **like accept**.
cc @fraenkel @odeke-em input...
chang consensus, accepted.
chang mention issue: `net/http: add transport.getproxyconnectheader`
chang mention issue: `doc/go1.16: mention net/http.transport.getproxyconnectheader`
