cmd/vet: ensur error implement is, as, unwrap anonym interfac
interfac `errors.is` `errors.as` seem stabl complete. unfortunately, requir read godoc ensur custom error type correctli implement `interface{ as(interface{}) bool }`, `interface{ is(error) bool }`, `interface{ unwrap() error }`. lead implement bug fail pathologically.
instead expos symbols, propos #39539, add vet check ensur `error` `is`, `as`, `unwrap` method implement correct interfaces. requir `error` implement `is`, `as`, `unwrap`, simpli symbol exists, syntax matches.
technically, required, use anonym type perform check compil time. ungainly, automatic, fail typograph issu due distribut implementation:
```go
var (
        _ = (interface{ As(interface{}) bool })(myError{})
        _ = (interface{ Is(error) bool })(myError{})
        _ = (interface{ Unwrap() error })(myError{})
        _ = error(myError{})
```
@ianlancetaylor: add propos project?
@carnott-snap done.
get lost incom stack?
incom stack 200 issu it, go take get all.
see .
propos ad activ column propos project
review weekli propos review meetings.
— rsc propos review group
object `cmd/vet` check signatur `as`/`is`/`unwrap` method type implement `error` interface; seem unlik signific fals positives. however, best way ensur error implement interfac correctli write test error, test valid method signatur implementation.
chang mention issue: `passes/stdmethods: warn error unexpect is, as, unwrap method.`
base discuss above, propos seem like **like accept**.
— rsc propos review group
test golang.org/cl/312829 larg go codebas use is, unwrap. find fals positives.
chang consensus, **accepted**. 🎉
