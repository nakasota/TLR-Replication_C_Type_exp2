testing/iotest: add errread
### abstract
add `iotest.errreader` `io.reader` implement read 0 byte fails.
would use quickli ad test case `read` work reason.
### background
`iotest` packag alreadi coupl `io.reader` implementations, fail right away.
nearli codebas work in, creat `failreader`/`errreader` implementation. judg github searches, alone.
### propos
add standard library, `iotest` package.
actual pr open already:
### rational
make easier add test case read failures.
call errreader, match dataerrreader, seem fine me.
is:
packag iotest
// errread return io.read return 0, err read calls.
func errreader(err error) io.read
`errreader` sound good well. updat proposal.
edit: updat pr well.
ad propos minutes. seem fine me, said before.
base trivial lack objections, seem like **like accept**.
chang consensus, accepted.
think "help wanted" tag got mangled?
@networkimprov thanks, fixed.
need anyth els get #34741 merged?
use addit stdlib.
wonder fail immedi return 0 byte read?
number byte read set tests, errread could potenti cover case
includ case immedi failur use errreader(0)
example:
```go
func ErrReader(n int) io.Reader {
	return &errReader{n}
}
type errReader struct{
       n int
}
func (r *errReader) Read(p []byte) (int, error) {
	return n, ErrIO
}
```
@caarlos0 make sense?
> @caarlos0 make sense?
sound good me, sure chang now, sinc approv as-is... cc/ @rsc
@caarlos0 need anyth moment. current releas freez 1.15. 1.15 releas goe out, review cl. thank send it.
person think stick simpl `errreader`. infinit number possibl permutations; want support them.
chang mention issue: `testing/iotest: add errreader`
chang mention issue: `testing/iotest: correct errread signatur remov export error`
chang mention issue: `net/http: use iotest.errread tests`
chang mention issue: `doc/go1.16: mention new testing/iotest functions`
