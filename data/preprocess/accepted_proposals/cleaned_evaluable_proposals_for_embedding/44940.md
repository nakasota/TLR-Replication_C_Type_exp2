unicode/utf16: add runelen
sometim use determin mani 16-bit word take encod unicod rune utf16. alreadi `utf8.runelen`, one utf16 could useful.
would look someth like (those privat constant insid utf16):
```golang
// RuneLen returns the number of 16-bit words required to encode the rune.
// It returns -1 if the rune is not a valid value to encode in UTF-16.
func RuneLen(r rune) int {
	switch {
	case 0 <= r && r < surr1, surr3 <= r && r < surrSelf:
		return 1
	case surrSelf <= r && r <= maxRune:
		return 2
	default:
		return -1
	}
}
```
see `utf8.encodedlen`. mean `encodedlen` `runelen`? `utf8.runelen`.
woop yes, meant runelen. fixed.
propos ad activ column propos project
review weekli propos review meetings.
— rsc propos review group
base discuss above, propos seem like **like accept**.
— rsc propos review group
chang consensus, **accepted**. 🎉
