encoding/csv: add reader.inputoffset method
```
// InputOffset returns the input stream byte offset of the current reader
// position. The offset gives the location of the end of the most recently
// read row and the beginning of the next row. 
func (r *Reader) InputOffset() int64
```
`encoding/json.decoder` alreadi method like this:

encoding/json packag made sens add `inputoffset`, straightforward way code call encoding/json provid `io.reader` know token read last. littl less convinc encoding/csv, line-ori format. `io.reader` keep track line processed, return line time.
still, symmetri packag reason argument.
make sens here, too. sometim necessari random access rows, read without read entir (possibl huge) file.
implement simpl invert index, document row csv files. everi document id map filenam row offset. queri index, read row match seek offsets.
there' also question stackoverflow:
propos ad activ column propos project
review weekli propos review meetings.
— rsc propos review group
base discuss above, propos seem like **like accept**.
— rsc propos review group
chang consensus, **accepted**. 🎉
