mime/multipart: add filecontentdisposit
upload file part multipart/form-data requests, specif allow content type specified, e.g. application/pdf. however, current version go alway use application/octet-stream. possibl work around this, requir duplic code like `sprintf` command escaping. exampl found example, see line 345-348 could replac singl line go api support custom content types, escap quot could remov completely.
alreadi creat pr which: fulli backward compat introduc new function; exist function deleg new one.
record, new api propos
```Go
// CreateFormFileWithContentType is a convenience wrapper around CreatePart.
// It creates a new form-data header with the provided field name, file name and content type.
func (w *Writer) CreateFormFileWithContentType(fieldname, filename, contentType string) (io.Writer, error)
```
instead add:
```
func FileContentDisposition(fieldname, filename string) string {
    return fmt.Sprintf(`form-data; name="%s"; filename="%s"`,
        escapeQuotes(fieldname), escapeQuotes(filename)))
}
```
?
createformfil simple:
```
func (w *Writer) CreateFormFile(fieldname, filename string) (io.Writer, error) {
        h := make(textproto.MIMEHeader)
        h.Set("Content-Disposition", FileContentDisposition(fieldname, filename)
        h.Set("Content-Type", "application/octet-stream")
        return w.CreatePart(h)
}
```
point want custom content-typ header, reason write version.
propos ad activ column propos project
review weekli propos review meetings.
— rsc propos review group
latter would work, still would requir 4 line code (exclud function declaration) use differ content type. think form file mostli come one variant - exist `createformfile` produces, often differ content types. that' `createformfilewithcontenttype` come in.
proposal, file upload make develop write this:
```
h := make(textproto.MIMEHeader)
h.Set("Content-Disposition", FileContentDisposition(fieldname, filename)
h.Set("Content-Type", "application/pdf")
writer, error := w.CreatePart(h)
// error check omitted
// now write to writer
```
instead this:
```
writer, error := CreateFormFileWithContentType(fieldname, filename, "application/pdf")
```
perhap `filecontentdisposition` good addit develop need add header like perhap content encod (base64). personally, add functions.
unusu enough case ok write 4 lines. import part copy-past complex sprintf. retitl filecontentdisposit function.
disagre content type `application/octet-stream` unusu case. sever servic verifi provid content type first shortcut; valid (e.g. imag pdf) actual content inspected.
said ok write 4 line case. part bad copy-past sprintf call, henc function.
base discuss above, propos seem like **like accept**.
— rsc propos review group
copi possibl altern #49329:
```
// NewFormFieldHeader creates a form-data header with the provided field name.
func NewFormFieldHeader(fieldname) textproto.MIMEHeader {
  h := make(textproto.MIMEHeader)
  h.Set("Content-Disposition", fmt.Sprintf(`form-data; name="%s"`, escapeQuotes(fieldname)))
  return h
}
// NewFormFileHeader creates a form-data header with the provided field and file names.
func NewFormFileHeader(fieldname, filename string) textproto.MIMEHeader {
  h := make(textproto.MIMEHeader)
  h.Set("Content-Disposition", fmt.Sprintf(`form-data; name="%s"; filename="%s"`, escapeQuotes(fieldname), escapeQuotes(filename)))
  return h
}
```
push header name (in addit value) `multipart` packag api.
soon there' second field write helper for, helper make map go scale. seem like probabl stick field-specif helper.
chang consensus, **accepted**. 🎉
