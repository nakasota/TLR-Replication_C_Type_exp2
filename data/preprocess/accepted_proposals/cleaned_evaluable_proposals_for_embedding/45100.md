net/url: add values.has(key)
current inbuilt method within valu detect whether queri paramet without valu set (e.g. `
propos ad follow method determin whether specif key set:
```
// Checks whether a given key is set.
func (v Values) IsSet(key string) bool {
	if _, ok := v[key]; ok {
		return true
	}
	return false
}
```
would also work queri paramet values.
reduc to?
```
func (v Values) IsSet(key string) bool {
	_, ok := v[key]
	return ok
}
```
instead write ```_, ok := v[key]``` propos ```v.isset(key)``` would abl use instead?
yep, concise, agree.
seem like usual go word (hasprefix, hassuffix, types.typeandvalue.hasok, etc).
otherwis seem fine - typic bother use map directly, ad oper method valu already.
propos ad activ column propos project
review weekli propos review meetings.
— rsc propos review group
5 thumb original, seem like one oper _haven't_ put valu api, might well.
anyon object ad values.has? so, why?
@rsc suppos due naming, `has` (a mention above) natur go.
base discuss above, propos seem like **like accept**.
— rsc propos review group
chang consensus, **accepted**. 🎉
