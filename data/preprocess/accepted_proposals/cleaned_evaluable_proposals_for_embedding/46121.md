html/template: make funcmap alia text/template.funcmap
discuss twitter recently, gener come around, `html/template` `text/template` api reason abstract singl gener interface, fact `funcmap` defin two separ type make bit harder could be.
propos `html/template`, `funcmap` shall defin alia follows:
```
package template
import "text/template"
type FuncMap = template.FuncMap
```
`html/template` alreadi import `text/template`, there' problem chang depend graph, can't current see potenti issu combin two types.
think reason user program could break change? imagin artifici cases, like requir two type differ via e.g. reflection, imagin anyon would that.
seem reason me. ad minutes. /cc @robpik
propos ad activ column propos project
review weekli propos review meetings.
— rsc propos review group
seem sound.
> imagin artifici cases, like requir two type differ via e.g. reflection, imagin anyon would that.
thought that, think realist case might problem, hyrum' law notwithstanding.
base discuss above, propos seem like **like accept**.
— rsc propos review group
chang consensus, **accepted**. 🎉
